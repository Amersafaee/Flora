import 'package:flutter/foundation.dart';

void main() {
  debugPrint('MANUAL TEST CHECKLIST — run these in Firebase Console Simulator');
  debugPrint('- authenticated user can read their own plants');
  debugPrint('- unauthenticated user cannot read plants');
  debugPrint('- user cannot read another user\'s plants');
  debugPrint('- anyone can read species');
  debugPrint('- authenticated user can create a post');
  debugPrint('- etc');
}
